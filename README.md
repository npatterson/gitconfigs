Prerequisites
------
* git 1.7+

Installation
------

1. Clone the repo to ~/.gitconfigs


2. Add the following to ~/.gitconfig

  <pre>
    [include]
        path = .gitconfigs/all
  </pre>

3. Verify install

  ```bash
  git verify
  ```